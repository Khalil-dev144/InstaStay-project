<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hotel Deals Finder</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/filter.css">
    <script defer src="/js/filter.js"></script>
</head>
<body>
    <div class="container mt-5">
        <div class="text-center mb-4">
            <h2 class="fw-bold text-primary">Find the Best Hotel Deals</h2>
            <p class="text-muted">Choose your budget and location to find the perfect stay.</p>
        </div>

        <!-- Filter Form -->
        <div class="card shadow-lg p-4">
            <div class="row flex-column justify-content-center align-items-center g-3">
                <!-- Price Input -->
                <div class="col-md-5">
                    <label class="form-label fw-semibold">💰 Price Range:</label>
                    <input type="number" class="form-control" id="priceInput" name="price" placeholder="Enter your budget" min="500" max="5000" step="10">
                </div>
                
                <!-- Location Input -->
                <div class="col-md-5">
                    <label class="form-label fw-semibold">📍 Location:</label>
                    <input type="text" class="form-control" id="locationInput" name="location" placeholder="Enter location (e.g., New York)">
                </div>
                
                <!-- Search Button -->
                <div class="col-md-2 d-flex align-items-end">
                    <button id="searchBtn" class="btn btn-primary w-100 rounded-pill">🔍 Search</button>
                </div>
            </div>
        </div>

        <!-- Room Listings -->
        <div id="roomList" class="row g-4 mt-4">
            <!-- Room Cards will be dynamically added here -->
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
